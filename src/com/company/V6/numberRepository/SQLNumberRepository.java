package com.company.V6.numberRepository;

public class SQLNumberRepository implements NumberRepository {
    public int[] read() throws NumberRepositoryException {
        //make the database connection
        //prepare SQL statement
        //execute the SQL statement against the database using the connection
        //get the result set and map it to numbers array
        //return that numbers array
        return new int[0];
    }
}
